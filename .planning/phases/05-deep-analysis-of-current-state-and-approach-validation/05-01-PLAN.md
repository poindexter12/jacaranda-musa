---
phase: 05-deep-analysis-of-current-state-and-approach-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test.just
autonomous: false
requirements:
  - HLTH-02

must_haves:
  truths:
    - "`just test::validate` includes an external URL check against musa-project-test.joeseymour.io via Cloudflare Tunnel"
    - "External URL check clearly reports PASS or FAIL with HTTP status"
  artifacts:
    - path: "test.just"
      provides: "External URL validation in validate recipe"
      contains: "musa-project-test.joeseymour.io"
  key_links:
    - from: "test.just (validate recipe)"
      to: "https://musa-project-test.joeseymour.io"
      via: "curl with timeout"
      pattern: "curl.*musa-project-test"
---

<objective>
Add external URL validation to `just test::validate` so Cloudflare Tunnel failures are detected automatically.

Purpose: The external access path (Cloudflare Tunnel -> SWAG -> Twenty) is the production-critical path, but `just test::validate` only checks internal localhost endpoints. External access is currently broken (DNS resolution failure per research). This plan adds the external check so it's impossible to miss tunnel failures in future validations.

Note: Actually FIXING the tunnel (DNS configuration in Cloudflare Dashboard) requires a human action, but the check itself is automated. The checkpoint only triggers if the automated check FAILS.

Context: The v1.17→v1.18 upgrade was motivated by a sidebar bug (native sidebar elements disappearing). The upgrade did NOT fix it. This plan also checks the running version and surfaces the sidebar bug for user verification.

Output: Updated `test.just` with external URL validation in the `validate` recipe.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@test.just
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add external URL check to validate recipe</name>
  <files>test.just</files>
  <action>
In `test.just`, add an "External Access" section to the `validate` recipe, AFTER the existing "Health Check" section. The external check should:

1. Use `curl -sf -o /dev/null -w 'HTTP %{http_code}' --max-time 10 https://musa-project-test.joeseymour.io` (run from the LOCAL machine, not via SSH — this tests the full external path).
2. Print `External (Cloudflare Tunnel): OK` on success or `External (Cloudflare Tunnel): FAIL` on failure.
3. Do NOT fail the entire validate recipe if external check fails — use `|| true` so internal checks still report. The external check is informational but clearly labeled.

Add a blank line separator and `echo "--- External Access ---"` header before the curl.

The domain `musa-project-test.joeseymour.io` should match what's in the CLAUDE.md docs (this is the Cloudflare Tunnel hostname, distinct from `twenty_domain` which is `musa-project-crm-test.joeseymour.io`).

Wait — check: the CLAUDE.md says the Tunnel public hostname is `musa-project-test.joeseymour.io` but the SWAG config uses `musa-project-crm-test.joeseymour.io` as the twenty_domain. The external URL should be the Tunnel hostname. Verify which one is actually configured in Cloudflare by checking CLAUDE.md architecture notes. Use `musa-project-test.joeseymour.io` as specified in CLAUDE.md Cloudflare Tunnel section.
  </action>
  <verify>
    <automated>grep -q "musa-project-test.joeseymour.io" test.just && grep -q "External Access" test.just && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>The `validate` recipe in test.just includes an external URL check against the Cloudflare Tunnel hostname with a 10-second timeout, clearly labeled output, and non-blocking failure mode.</done>
</task>

<task type="auto">
  <name>Task 2: Run external URL check and report result</name>
  <files>test.just</files>
  <action>
Run `just test::validate` to exercise the new external URL check from Task 1. Capture the output and report the result.

If the external check PASSES: report success and move on.
If the external check FAILS: report the failure clearly with the HTTP status or error, and note that the user will need to check Cloudflare Dashboard (Zero Trust → Networks → Tunnels → `musa-project-test`) to fix the tunnel. Do NOT block — this is informational.

Additionally, check if the Twenty CRM v1.18 sidebar bug persists. The upgrade from v1.17 to v1.18 was motivated by native sidebar elements disappearing. SSH to the LXC and check the server container is running v1.18:
```
ssh root@musa-test.lan "docker inspect server --format '{{.Config.Image}}'"
```
Report the running image version. The sidebar bug itself requires visual verification by the user later — just confirm the version here.
  </action>
  <verify>
    <automated>just test::validate 2>&1 | grep -E "External|PASS|FAIL" && echo "CHECK RAN" || echo "CHECK MISSING"</automated>
  </verify>
  <done>External URL check ran and result reported (PASS or FAIL). Twenty CRM container version confirmed. If external check failed, the user has clear next steps for Cloudflare Dashboard investigation.</done>
</task>

<task type="checkpoint:human-verify" gate="soft">
  <name>Task 3: User verifies Cloudflare Tunnel and sidebar bug (only if Task 2 reported failures)</name>
  <action>SKIP this checkpoint if Task 2 reported external check PASS. Only engage if the external URL check FAILED.</action>
  <what-built>External URL validation runs automatically. This checkpoint exists only as a fallback if the tunnel is broken and needs manual Cloudflare Dashboard intervention.</what-built>
  <how-to-verify>
Only if external check FAILED:
1. Go to Cloudflare Dashboard → Zero Trust → Networks → Tunnels → `musa-project-test`
2. Verify tunnel exists and is "Connected"
3. Verify public hostname: `musa-project-test.joeseymour.io` → `http://localhost:80`
4. Fix configuration, then re-run `just test::validate` to confirm

Separately (regardless of tunnel status): visit the Twenty CRM UI and check whether the native sidebar elements are still disappearing. This was the primary motivation for the v1.17→v1.18 upgrade and the upgrade did NOT fix it. Confirm whether the sidebar bug persists in v1.18.
  </how-to-verify>
  <resume-signal>Type "approved" if tunnel works (or was already working), and note sidebar bug status. Or describe Cloudflare Dashboard findings.</resume-signal>
</task>

</tasks>

<verification>
- `just test::validate` runs and includes an "External Access" section
- External check uses curl with a timeout (not hanging)
- Output clearly shows PASS/FAIL for the Cloudflare Tunnel path
</verification>

<success_criteria>
- HLTH-02 satisfied: `just test::validate` includes external URL check via Cloudflare Tunnel
- External access either works (tunnel configured) or the failure is clearly surfaced
</success_criteria>

<output>
After completion, create `.planning/phases/05-deep-analysis-of-current-state-and-approach-validation/05-01-SUMMARY.md`
</output>
