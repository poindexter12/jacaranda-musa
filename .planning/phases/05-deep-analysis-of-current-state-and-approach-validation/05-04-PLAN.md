---
phase: 05-deep-analysis-of-current-state-and-approach-validation
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified: []
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "`just test::deploy` completes without errors on the existing LXC"
    - "Running `just test::deploy` a second time produces no spurious changes (idempotent)"
    - "`just test::validate` passes all checks (internal + external)"
  artifacts: []
  key_links:
    - from: "just test::deploy"
      to: "ansible-playbook"
      via: "justfile recipe"
      pattern: "ansible-playbook.*deploy.yaml"
---

<objective>
Validate the full deployment pipeline end-to-end with all hardening changes applied.

Purpose: Plans 01-03 modified templates, health checks, and recipes. This plan runs the actual deployment to verify everything works together — the configs render correctly, health checks pass with tighter timeouts, and the pipeline is idempotent.

Output: Verified deployment with all Phase 5 changes applied. Pipeline idempotency confirmed.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deep-analysis-of-current-state-and-approach-validation/05-02-SUMMARY.md
@.planning/phases/05-deep-analysis-of-current-state-and-approach-validation/05-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run deploy and validate full pipeline with hardening changes</name>
  <action>Human runs the deployment pipeline and validates that all Phase 5 changes work together. Tests idempotency by running deploy twice.</action>
  <what-built>Plans 01-03 made the following changes:
- External URL validation added to `just test::validate`
- GHCR images parameterized (backup_tag, rollup_tag, webhook_tag)
- Health check intervals tightened (10s interval, 5 retries, curl timeouts)
- Domain vars consolidated to group_vars only
- Rollback recipe added
- Secrets documented

Now the deployment pipeline needs to run to apply these changes to the live LXC.</what-built>
  <how-to-verify>
1. **Deploy:** Run `just test::deploy` — this applies all template changes to the LXC.
   - Expect: Ansible completes successfully, health checks pass with new tighter timeouts.
   - The rollback-prep step should save current config before overwriting.
2. **Validate:** Run `just test::validate` — should show all 9 containers + internal health + external access.
3. **Idempotency:** Run `just test::deploy` a SECOND time.
   - Expect: No "changed" tasks (templates unchanged, stack already running).
   - The output should show all tasks as "ok" except the "Start Docker Compose stack" which always reports changed_when: false.
4. **Validate again:** `just test::validate` still passes.
5. **Check rollback readiness:** `ssh root@musa-test.lan "ls -la /opt/musa/*.bak"` — should show docker-compose.yaml.bak and .env.bak.
  </how-to-verify>
  <resume-signal>Type "approved" if deploy + validate + idempotency all pass, or describe failures.</resume-signal>
</task>

</tasks>

<verification>
- `just test::deploy` completes without errors
- `just test::validate` shows all green (internal + external)
- Second `just test::deploy` run shows no spurious changes
- Rollback .bak files exist on the LXC
</verification>

<success_criteria>
- PIPE-01: `just test::deploy` runs end-to-end without errors (Terraform already applied, this is Ansible-only)
- PIPE-02: `just test::deploy` re-deploys idempotently on existing LXC
- All 9 containers healthy after deployment with new configuration
</success_criteria>

<output>
After completion, create `.planning/phases/05-deep-analysis-of-current-state-and-approach-validation/05-04-SUMMARY.md`
</output>
