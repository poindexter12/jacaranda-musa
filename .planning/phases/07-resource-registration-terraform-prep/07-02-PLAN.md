---
phase: 07-resource-registration-terraform-prep
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - terraform/main.tf
  - terraform/variables.tf
  - terraform/envs/test/main.tf
  - .planning/phases/07-resource-registration-terraform-prep/07-02-SUMMARY.md
autonomous: false
requirements: [PROV-01]

must_haves:
  truths:
    - "tofu plan for test environment completes without referencing CA cert signing module or variables"
    - "No CA-related variables, module calls, or outputs remain in musa Terraform configuration"
    - "Shared LXC module accepts ssh_user_ca_pubkey as optional (empty string skips CA provisioners)"
  artifacts:
    - path: "terraform/variables.tf"
      provides: "Musa module variables without ssh_user_ca_pubkey"
    - path: "terraform/main.tf"
      provides: "Musa module with no ssh_user_ca_pubkey in LXC module calls"
    - path: "terraform/envs/test/main.tf"
      provides: "Test env without ssh_user_ca_pubkey passthrough"
  key_links:
    - from: "terraform/main.tf"
      to: "lib/infrastructure/terraform/modules/lxc/main.tf"
      via: "module call without ssh_user_ca_pubkey argument"
      pattern: "module.*lxc_app"
---

<objective>
Remove CA cert signing dependency from musa Terraform configuration so that LXC provisioning no longer requires step-ca infrastructure.

Purpose: Per-environment SSH key pairs replace CA-based signing. The shared LXC module must be updated first (in jacaranda-shared-libs) to make CA signing optional, then musa stops passing CA variables. This unblocks Phase 8 LXC provisioning without requiring step-ca.

Output: Musa Terraform files with zero CA references; `tofu validate` passes after shared module update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- The shared LXC module's current variable signature (from lib/) -->
<!-- After the shared-libs update, ssh_user_ca_pubkey will have default = "" -->
<!-- and all CA-related null_resources will be conditional on it being non-empty -->

From lib/infrastructure/terraform/modules/lxc/variables.tf:
```hcl
variable "ssh_user_ca_pubkey" {
  description = "SSH User CA public key for cert-based authentication"
  type        = string
  # CURRENTLY required (no default) -- will become optional with default = ""
}
```

From terraform/variables.tf (musa module):
```hcl
variable "ssh_user_ca_pubkey" {
  description = "SSH User CA public key for certificate authentication"
  type        = string
}
```

From terraform/main.tf (two module calls both pass it):
```hcl
module "lxc_app" {
  ssh_user_ca_pubkey = var.ssh_user_ca_pubkey  # LINE 86 - REMOVE
}
module "lxc_bak" {
  ssh_user_ca_pubkey = var.ssh_user_ca_pubkey  # LINE 116 - REMOVE
}
```

From terraform/envs/test/main.tf:
```hcl
module "musa" {
  ssh_user_ca_pubkey = local.base.ssh_user_ca_pubkey  # LINE 119 - REMOVE
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Update shared LXC module to make CA signing optional</name>
  <action>
The shared LXC module in jacaranda-shared-libs must be updated BEFORE musa can remove CA references. The following changes are needed in the jacaranda-shared-libs repository:

**File: lib/infrastructure/terraform/modules/lxc/variables.tf**
- Change `ssh_user_ca_pubkey` variable to have `default = ""`

**File: lib/infrastructure/terraform/modules/lxc/main.tf**
- Wrap `null_resource.sign_host_cert` with: `for_each = var.ssh_user_ca_pubkey != "" ? var.instances : {}`
- Wrap `null_resource.configure_ssh_ca` with: `for_each = var.ssh_user_ca_pubkey != "" ? var.instances : {}`
- Wrap `null_resource.verify_ssh` with: `for_each = var.ssh_user_ca_pubkey != "" ? var.instances : {}`
- Update `null_resource.ha_add` depends_on to handle optional verify_ssh (use `depends_on = [proxmox_lxc.container]` when CA is skipped)

After making these changes in jacaranda-shared-libs:
1. Commit and tag a new version (e.g., v1.5.0)
2. In the jacaranda-musa repo, update the submodule: `cd lib && git fetch && git checkout v1.5.0 && cd .. && git add lib`
  </action>
  <resume-signal>Type "done" after shared-libs is updated and lib/ submodule is bumped to the new version, or provide the new version tag</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove CA references from musa Terraform configuration</name>
  <files>terraform/variables.tf, terraform/main.tf, terraform/envs/test/main.tf</files>
  <read_first>
    - terraform/variables.tf (current state -- contains ssh_user_ca_pubkey variable at lines 55-58)
    - terraform/main.tf (current state -- passes ssh_user_ca_pubkey on lines 86 and 116)
    - terraform/envs/test/main.tf (current state -- passes ssh_user_ca_pubkey on line 119)
    - lib/infrastructure/terraform/modules/lxc/variables.tf (verify ssh_user_ca_pubkey now has default = "")
  </read_first>
  <action>
**terraform/variables.tf** — Remove the `ssh_user_ca_pubkey` variable block entirely (lines 55-58):
```hcl
# DELETE THIS BLOCK:
variable "ssh_user_ca_pubkey" {
  description = "SSH User CA public key for certificate authentication"
  type        = string
}
```

**terraform/main.tf** — Remove the `ssh_user_ca_pubkey` argument from BOTH module calls:

In `module "lxc_app"` block (around line 86), delete:
```hcl
  ssh_user_ca_pubkey = var.ssh_user_ca_pubkey
```

In `module "lxc_bak"` block (around line 116), delete:
```hcl
  ssh_user_ca_pubkey = var.ssh_user_ca_pubkey
```

Both module calls should retain all other arguments: `vlans`, `ssh_public_key`, `dns_server`, `ostemplate`, `storage`, `cores`, `memory`, `disk_size`, `nesting`.

**terraform/envs/test/main.tf** — Remove the `ssh_user_ca_pubkey` argument from the `module "musa"` block (around line 119), delete:
```hcl
  ssh_user_ca_pubkey = local.base.ssh_user_ca_pubkey
```

The musa module call should retain: `env`, `instances`, `ansible_inventory_path`, `vlans`, `ssh_public_key`, `dns_server`, `ostemplate`, `storage`, `cores`, `memory`, `disk_size`.

After all edits, run `terraform fmt` on each modified file to ensure formatting compliance.
  </action>
  <verify>
    <automated>cd terraform && grep -rn "ssh_user_ca_pubkey" variables.tf main.tf envs/test/main.tf 2>/dev/null | wc -l | grep -q "^0$" && echo "PASS: no CA references" || echo "FAIL: CA references still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ssh_user_ca_pubkey" terraform/variables.tf` returns 0
    - `grep -c "ssh_user_ca_pubkey" terraform/main.tf` returns 0
    - `grep -c "ssh_user_ca_pubkey" terraform/envs/test/main.tf` returns 0
    - `grep -rn "ssh_user_ca_pubkey" terraform/` returns no results
    - `terraform fmt -check terraform/main.tf` exits 0 (properly formatted)
    - `terraform fmt -check terraform/variables.tf` exits 0
    - `terraform fmt -check terraform/envs/test/main.tf` exits 0
    - terraform/main.tf still contains `ssh_public_key = var.ssh_public_key` (only CA key removed, not SSH public key)
    - terraform/main.tf still contains both `module "lxc_app"` and `module "lxc_bak"` blocks
  </acceptance_criteria>
  <done>All CA-related variables, arguments, and passthroughs removed from musa Terraform. Only per-environment SSH public key auth remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate Terraform with updated shared module</name>
  <what-built>Removed all ssh_user_ca_pubkey references from musa Terraform configuration (variables.tf, main.tf, envs/test/main.tf). The shared LXC module now defaults ssh_user_ca_pubkey to empty string, skipping CA signing provisioners.</what-built>
  <how-to-verify>
1. Verify the shared module update is in place: `grep 'default' lib/infrastructure/terraform/modules/lxc/variables.tf | grep ssh_user_ca_pubkey`
   - Expected: line containing `default = ""`
2. Run Terraform validation: `cd terraform/envs/test && tofu validate`
   - Expected: "Success! The configuration is valid."
   - If this fails with "missing required variable ssh_user_ca_pubkey": the lib/ submodule was not bumped to the new version. Run `cd lib && git fetch && git checkout <new-tag> && cd ../.. && git add lib`
3. Run Terraform plan (dry run): `just test::plan` or `cd terraform/envs/test && tofu plan`
   - Expected: plan output should NOT contain any references to `sign_host_cert`, `configure_ssh_ca`, or `verify_ssh` null_resources
   - Expected: plan should show LXC container resources only
4. Verify no CA references anywhere: `grep -rn "ssh_user_ca_pubkey\|sign_host_cert\|step_ca_host" terraform/`
   - Expected: no results
  </how-to-verify>
  <resume-signal>Type "approved" if tofu validate succeeds and plan shows no CA resources, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared-module -> musa-module | Shared LXC module interface change; musa stops passing CA variable |
| terraform-config -> proxmox-api | LXC provisioning without CA signing; SSH access via public key only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Elevation of Privilege | SSH auth | mitigate | Per-environment SSH key pairs replace CA certs; ssh_public_key still configured on LXC containers for root access |
| T-07-04 | Denial of Service | LXC provisioning | accept | Removing CA signing simplifies provisioning (fewer external dependencies); step-ca outage no longer blocks LXC creation |
| T-07-05 | Tampering | Terraform state | mitigate | tofu validate + tofu plan dry run before any apply; human-verify checkpoint confirms plan output |
</threat_model>

<verification>
- `grep -rn "ssh_user_ca_pubkey" terraform/` returns 0 results
- `grep -rn "step_ca" terraform/` returns 0 results
- `cd terraform/envs/test && tofu validate` exits 0
- `tofu plan` output contains no `sign_host_cert`, `configure_ssh_ca`, or `verify_ssh` resources
- `terraform/main.tf` still contains `ssh_public_key = var.ssh_public_key` (public key auth preserved)
</verification>

<success_criteria>
PROV-01 satisfied: Terraform LXC module calls no longer depend on CA cert signing. No CA-related variables, module calls, or outputs remain in musa Terraform configuration. `tofu validate` passes. `tofu plan` shows no CA-related resources.
</success_criteria>

<output>
After completion, create `.planning/phases/07-resource-registration-terraform-prep/07-02-SUMMARY.md`
</output>
