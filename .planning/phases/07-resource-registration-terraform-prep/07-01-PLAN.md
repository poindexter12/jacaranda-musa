---
phase: 07-resource-registration-terraform-prep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-resource-registration-terraform-prep/07-01-SUMMARY.md
autonomous: false
requirements: [REG-01, REG-02, REG-03]

must_haves:
  truths:
    - "Registry contains VMID entries for 1190, 1191, 1192 attributed to musa service"
    - "Registry contains mgmt VLAN IP entries for 192.168.5.190, .191, .192"
    - "Registry contains transfer VLAN IP entries for 192.168.11.190, .191, .192"
    - "registry check passes without errors for musa service"
  artifacts:
    - path: "../jacaranda-inventory/services/musa.yaml"
      provides: "Musa service registry with VMID + IP allocations"
      contains: "vmid:"
  key_links:
    - from: "terraform/envs/test/main.tf"
      to: "../jacaranda-inventory/services/musa.yaml"
      via: "VMID and IP values match between Terraform config and registry"
      pattern: "vmid.*=.*119[012]"
---

<objective>
Verify and formalize musa resource allocations (VMIDs and IPs) in the jacaranda-inventory registry.

Purpose: The network registry is the authoritative source of truth for resource allocations. Musa's VMID and IP allocations must be first-class registry entries (not just Terraform config comments) so that tooling can query, validate, and prevent collisions.

Output: Registry entries with `vmid` field on all 6 musa allocations, verified via `registry check`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@terraform/envs/test/main.tf

This plan modifies files in the jacaranda-inventory repository (../jacaranda-inventory/), NOT the musa repository. The registry is the external source of truth for network allocations.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vmid field to musa registry allocations</name>
  <files>../jacaranda-inventory/services/musa.yaml</files>
  <read_first>
    - ../jacaranda-inventory/services/musa.yaml (current musa allocations -- VMIDs are only in notes fields)
    - ../jacaranda-inventory/services/ca.yaml (reference for allocation entry structure and conventions)
    - ../jacaranda-inventory/CLAUDE.md (registry schema and guardrails)
    - terraform/envs/test/main.tf (authoritative VMID/IP/hostname/node mapping)
  </read_first>
  <action>
Update `../jacaranda-inventory/services/musa.yaml` to add a `vmid` field to each of the 6 allocation entries. The `vmid` field goes after `role` and before `status` in each entry. The VMID values come from `terraform/envs/test/main.tf`:

Allocation entry updates (add `vmid:` line to each):

1. host 190, vlan mgmt, hostname test.app1.app.musa -> add `vmid: 1190`
2. host 191, vlan mgmt, hostname test.app2.app.musa -> add `vmid: 1191`
3. host 192, vlan mgmt, hostname test.bak.backup.musa -> add `vmid: 1192`
4. host 190, vlan transfer, hostname test.app1.app.musa -> add `vmid: 1190`
5. host 191, vlan transfer, hostname test.app2.app.musa -> add `vmid: 1191`
6. host 192, vlan transfer, hostname test.bak.backup.musa -> add `vmid: 1192`

Also add `node:` field after `vmid:` for each entry to track Proxmox node assignment:
1. test.app1.app.musa -> `node: joseph`
2. test.app2.app.musa -> `node: everette`
3. test.bak.backup.musa -> `node: maxwell`

Update the `notes:` field on each entry to remove the "(VMID XXXX)" suffix since VMID is now a first-class field. Simplify notes to just the role description, e.g.:
- "Twenty CRM app node on joseph" (drop "(VMID 1190)")
- "Twenty CRM app node on everette" (drop "(VMID 1191)")
- "Twenty CRM backup node on maxwell" (drop "(VMID 1192)")
- Transfer NIC entries: "Transfer NIC for app node on joseph" etc.

Preserve the existing `service:`, `owner:`, `description:`, `ranges:` header fields exactly as they are. Do not add or remove allocation entries -- only add fields to existing entries.

Note: The `vmid` and `node` fields are not in the Go Allocation struct. They will be preserved as YAML but ignored by the Go tooling until the struct is updated. This is acceptable -- the registry's CLAUDE.md says "YAML files are the data store" and the Go code is a consumer, not the schema authority. Run `registry check` after editing to confirm no validation errors are introduced.
  </action>
  <verify>
    <automated>cd ../jacaranda-inventory && grep -c "vmid:" services/musa.yaml | grep -q "6" && echo "PASS: 6 vmid fields found" || echo "FAIL: expected 6 vmid fields"</automated>
  </verify>
  <acceptance_criteria>
    - services/musa.yaml contains exactly 6 lines matching `vmid:` (one per allocation entry)
    - services/musa.yaml contains `vmid: 1190` (twice -- mgmt and transfer for app1)
    - services/musa.yaml contains `vmid: 1191` (twice -- mgmt and transfer for app2)
    - services/musa.yaml contains `vmid: 1192` (twice -- mgmt and transfer for bak)
    - services/musa.yaml contains `node: joseph` (twice -- mgmt and transfer for app1)
    - services/musa.yaml contains `node: everette` (twice -- mgmt and transfer for app2)
    - services/musa.yaml contains `node: maxwell` (twice -- mgmt and transfer for bak)
    - No notes field contains "(VMID" -- VMID info moved to dedicated field
  </acceptance_criteria>
  <done>All 6 musa allocation entries have vmid and node as first-class fields. VMID info removed from notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify registry passes validation</name>
  <what-built>Added vmid and node fields to all 6 musa allocation entries in jacaranda-inventory registry. Existing IP allocations (REG-02, REG-03) were already present -- only VMID formalization (REG-01) was missing.</what-built>
  <how-to-verify>
1. Review the diff: `cd ../jacaranda-inventory && git diff services/musa.yaml`
2. Run registry validation: `cd ../jacaranda-inventory && registry check`
   - Expected: exit 0 (clean) or exit 2 (warnings-only about unknown fields `vmid`/`node`)
   - If exit 1 (errors): the `vmid`/`node` fields may need to be added to the Go Allocation struct first. Report back.
3. Verify VMID-to-IP consistency: confirm these mappings match terraform/envs/test/main.tf:
   - VMID 1190 -> 192.168.5.190 (mgmt) + 192.168.11.190 (transfer) -> joseph
   - VMID 1191 -> 192.168.5.191 (mgmt) + 192.168.11.191 (transfer) -> everette
   - VMID 1192 -> 192.168.5.192 (mgmt) + 192.168.11.192 (transfer) -> maxwell
4. If satisfied, commit changes in jacaranda-inventory: `cd ../jacaranda-inventory && git add services/musa.yaml && git commit -m "feat: add vmid and node fields to musa allocations (REG-01)"`
  </how-to-verify>
  <resume-signal>Type "approved" if registry check passes and diff looks correct, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| musa-repo -> inventory-repo | Cross-repo modification; allocations must match Terraform source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | services/musa.yaml | mitigate | Human-verify checkpoint confirms diff before commit; registry check validates structure |
| T-07-02 | Information Disclosure | VMID/IP allocations | accept | Network topology is internal-only; registry is private repo |
</threat_model>

<verification>
- `grep -c "vmid:" ../jacaranda-inventory/services/musa.yaml` returns 6
- `grep -c "node:" ../jacaranda-inventory/services/musa.yaml` returns 6
- `cd ../jacaranda-inventory && registry check` exits 0 or 2 (no errors)
- All 3 VMIDs (1190, 1191, 1192) present as first-class fields
- All 6 IPs (3 mgmt + 3 transfer) still present and unchanged
</verification>

<success_criteria>
REG-01 satisfied: VMID allocations 1190/1191/1192 are first-class registry fields attributed to musa service.
REG-02 satisfied: mgmt VLAN IP entries 192.168.5.190-.192 confirmed present (pre-existing).
REG-03 satisfied: transfer VLAN IP entries 192.168.11.190-.192 confirmed present (pre-existing).
Registry validation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-resource-registration-terraform-prep/07-01-SUMMARY.md`
</output>
