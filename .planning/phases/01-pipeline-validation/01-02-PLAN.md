---
phase: 01-pipeline-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "just check-secrets passes for all 7 1Password items"
    - "just test::full completes without errors (Terraform apply + Ansible deploy)"
    - "just test::deploy re-runs idempotently on existing LXC without spurious changes"
    - "just test::validate confirms all 9 containers healthy"
  artifacts: []
  key_links:
    - from: "just test::full"
      to: "LXC + Docker stack"
      via: "Terraform apply + Ansible deploy"
      pattern: "test::full"
    - from: "just test::deploy"
      to: "Ansible idempotent run"
      via: "No changed tasks on second run"
      pattern: "test::deploy"
---

<objective>
Run the full deployment pipeline and validate the repo split didn't break anything.

Purpose: This is the core validation. Run every key command and confirm results match Phase 1 success criteria from the roadmap. All four PIPE requirements are validated here.

Output: Human-verified confirmation that the pipeline works end-to-end.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-validation/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify secrets access (PIPE-04)</name>
  <files>justfile</files>
  <action>
    Human runs `just check-secrets` to verify all 7 1Password items are accessible.
    Updated check-secrets recipe with aligned 1Password paths (from Plan 01).
  </action>
  <verify>
    <automated>echo "CHECKPOINT — human must run: just check-secrets"</automated>
  </verify>
  <done>All 7 1Password items show "OK" (green) in check-secrets output</done>
  <how-to-verify>
    1. Run `just check-secrets`
    2. Confirm all 7 items show "OK" (green)
    3. If any show "MISSING", resolve the 1Password item before proceeding
  </how-to-verify>
  <resume-signal>Paste output showing all 7 items OK, or describe which items are missing</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run full pipeline and validate (PIPE-01, PIPE-02, PIPE-03)</name>
  <files>test.just</files>
  <action>
    Human runs the full pipeline: `just test::full`, then `just test::deploy` (idempotency check), then `just test::validate` (health check).
    Validates repo split didn't break the Terraform + Ansible pipeline.
  </action>
  <verify>
    <automated>echo "CHECKPOINT — human must run: just test::full, just test::deploy, just test::validate"</automated>
  </verify>
  <done>test::full exits 0, test::deploy shows zero changes on re-run, test::validate shows 9/9 containers healthy</done>
  <how-to-verify>
    **Step 1 — Full deployment (PIPE-01):**
    1. Run `just test::full`
    2. Confirm Terraform apply completes (LXC created/updated)
    3. Confirm Ansible deploy completes (all tasks green/ok)
    4. Note any errors or warnings

    **Step 2 — Idempotent re-deploy (PIPE-02):**
    1. Run `just test::deploy`
    2. Confirm Ansible reports zero changed tasks (or only expected changes like log timestamps)
    3. No containers restarted unnecessarily

    **Step 3 — Health validation (PIPE-03):**
    1. Run `just test::validate`
    2. Confirm all 9 containers show "OK running"
    3. Confirm Twenty CRM healthz shows "OK"
    4. Confirm SWAG port 80 shows "OK"

    **Step 4 — Spot check:**
    1. Run `ssh root@musa-test.lan "docker ps --format 'table {{.Names}}\t{{.Status}}'"` to see all containers with uptime
    2. Confirm no containers are restarting/crash-looping
  </how-to-verify>
  <resume-signal>Paste output from each step, or describe any failures encountered</resume-signal>
</task>

</tasks>

<verification>
- All 7 1Password items accessible
- `just test::full` exits 0
- `just test::deploy` (second run) shows zero meaningful changes
- `just test::validate` shows 9/9 containers healthy + healthz OK + SWAG OK
</verification>

<success_criteria>
All four PIPE requirements validated:
- PIPE-01: test::full runs end-to-end without errors
- PIPE-02: test::deploy idempotent on second run
- PIPE-03: all 9 containers healthy in validate output
- PIPE-04: all 7 secrets accessible via check-secrets
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-validation/01-02-SUMMARY.md`
</output>
