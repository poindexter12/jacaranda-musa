---
phase: 01-pipeline-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test.just
  - justfile
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "validate recipe checks all 9 container names, not just 5"
    - "check-secrets recipe verifies the same 1Password paths that deploy actually reads"
  artifacts:
    - path: "test.just"
      provides: "validate recipe with all 9 containers checked"
      contains: "twenty-backup"
    - path: "justfile"
      provides: "check-secrets aligned with deploy recipe 1Password paths"
      contains: "musa-project-crm-test/cf_api_token"
  key_links:
    - from: "justfile (check-secrets)"
      to: "test.just (deploy)"
      via: "1Password item paths must match"
      pattern: "op://Homelab/"
---

<objective>
Fix the validate recipe and check-secrets recipe so they accurately reflect the deployed stack before running pipeline validation.

Purpose: The validate recipe only checks 5 of 9 containers (missing twenty-backup, twenty-rollup, twenty-webhook-receiver, twenty-webhook-worker). The check-secrets recipe references `cloudflare/api_token` but the deploy recipe reads `musa-project-crm-test/cf_api_token` -- a path mismatch that means secrets check could pass while deploy fails.

Output: Updated `test.just` with all 9 containers in validate, updated `justfile` with aligned 1Password paths in check-secrets.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@test.just
@justfile
@ansible/roles/musa/templates/docker-compose.yaml.j2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing containers to validate recipe and fix check-secrets paths</name>
  <files>test.just, justfile</files>
  <action>
  **In test.just — validate recipe:**
  The `for container in` loop currently lists: `twenty-swag server worker db redis`
  Update to include all 9 Docker Compose service container names: `twenty-swag server worker db redis twenty-backup twenty-rollup twenty-webhook-receiver twenty-webhook-worker`

  Note: The compose file uses `container_name` for some services (twenty-swag, twenty-backup, twenty-rollup, twenty-webhook-receiver, twenty-webhook-worker) but not for server, worker, db, redis. Docker Compose auto-names those as `{project}-{service}-1`. Check the current validate recipe -- it uses raw names like `server` which works with `grep -q` against `docker ps` output. Keep the same pattern but add the 4 missing containers.

  **In justfile — check-secrets recipe:**
  The deploy recipe in test.just reads `op://Homelab/musa-project-crm-test/cf_api_token` but check-secrets verifies `Homelab/cloudflare/api_token`. Replace `Homelab/cloudflare/api_token` with `Homelab/musa-project-crm-test/cf_api_token` in the items array so the check matches what deploy actually uses.

  Leave all other items unchanged -- the remaining 6 items appear consistent between check-secrets and deploy.
  </action>
  <verify>
    <automated>grep -q "twenty-backup" test.just && grep -q "twenty-rollup" test.just && grep -q "twenty-webhook-receiver" test.just && grep -q "twenty-webhook-worker" test.just && grep -q "musa-project-crm-test/cf_api_token" justfile && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
  - validate recipe loops over all 9 container names
  - check-secrets verifies the same cf_api_token path that deploy reads
  </done>
</task>

</tasks>

<verification>
- `grep "twenty-backup" test.just` returns a match
- `grep "twenty-webhook-worker" test.just` returns a match
- `grep "musa-project-crm-test/cf_api_token" justfile` returns a match
- `grep "cloudflare/api_token" justfile` returns NO match (old path removed)
</verification>

<success_criteria>
validate recipe checks all 9 containers; check-secrets paths match deploy recipe paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-validation/01-01-SUMMARY.md`
</output>
