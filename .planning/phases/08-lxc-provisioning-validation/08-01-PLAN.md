---
phase: 08-lxc-provisioning-validation
plan: "01"
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - .planning/phases/08-lxc-provisioning-validation/08-01-SUMMARY.md
autonomous: false
requirements: [PROV-02, PROV-03]

must_haves:
  truths:
    - "tofu apply creates 3 LXC containers (VMIDs 1190, 1191, 1192) on joseph, everette, maxwell"
    - "ssh root@test.app1.app.musa.lan connects using musa-test key pair"
    - "SSH connectivity confirmed to all 3 hosts"
    - "docker ps runs without error on all 3 hosts"
  artifacts: []
  key_links:
    - from: "terraform/envs/test/main.tf"
      to: "ansible/inventory/test.yaml"
      via: "tofu apply generates Ansible inventory from instance map"
      pattern: "ansible_inventory_path"
---

<objective>
Provision 3-node test LXC cluster and validate SSH + Docker connectivity on all nodes.

Purpose: This is the physical provisioning step that creates the infrastructure defined in Phase 6 and cleaned up in Phase 7. After this, the 3 LXCs exist on Proxmox and are ready for Ansible-based service deployment.

Output: 3 running LXCs accessible via SSH, with Docker functional inside each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@terraform/envs/test/main.tf
@test.just

This plan is entirely human-action checkpoints — all tasks require running commands against live Proxmox infrastructure and validating real network connectivity. No code changes needed.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Initialize and apply Terraform</name>
  <action>
Run the Terraform init and apply sequence to create the 3 test LXC containers:

1. Initialize OpenTofu:
   ```bash
   just test::init
   ```

2. Preview changes:
   ```bash
   just test::plan
   ```
   Expected: Plan shows creation of 3 `proxmox_lxc.container` resources (VMIDs 1190, 1191, 1192).
   Expected: NO `null_resource.sign_host_cert`, `null_resource.configure_ssh_ca`, or `null_resource.verify_ssh` resources in the plan (CA signing skipped because ssh_user_ca_pubkey defaults to "").

3. Apply:
   ```bash
   just test::apply
   ```
   Expected: 3 LXC containers created. Custom multi-group Ansible inventory written to `ansible/inventory/test.yaml`.

4. Wait 30 seconds for containers to boot fully.
  </action>
  <resume-signal>Type "done" after tofu apply succeeds and 3 LXCs are created, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Validate SSH connectivity and Docker</name>
  <action>
Run the validation suite to confirm all 3 nodes are accessible and Docker is functional:

1. Run preflight DNS + SSH check:
   ```bash
   just test::verify
   ```
   Expected: All 3 hostnames resolve via DNS. SSH connects to all 3 hosts.

2. Run full validation:
   ```bash
   just test::validate
   ```
   Expected: Docker daemon OK, Compose plugin OK on all 3 hosts. No containers running yet (expected — service deployment is later).

3. Manual SSH test to each host:
   ```bash
   ssh root@test.app1.app.musa.lan hostname
   ssh root@test.app2.app.musa.lan hostname
   ssh root@test.bak.backup.musa.lan hostname
   ```
   Expected: Each returns its hostname.

4. Docker functionality test:
   ```bash
   ssh root@test.app1.app.musa.lan "docker ps"
   ssh root@test.app2.app.musa.lan "docker ps"
   ssh root@test.bak.backup.musa.lan "docker ps"
   ```
   Expected: Empty container list (no error). Docker is functional inside each LXC.
  </action>
  <resume-signal>Type "approved" if all 3 nodes pass SSH + Docker validation, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terraform -> proxmox-api | LXC creation via Proxmox API; containers get SSH keys from base-infra |
| ssh-client -> lxc-containers | SSH auth via public key pair (no CA) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | SSH auth | mitigate | Per-environment SSH key pairs; known_hosts checked on first connect |
| T-08-02 | Tampering | LXC config | accept | Terraform manages LXC config; manual changes via Proxmox UI discouraged per CLAUDE.md |
| T-08-03 | Denial of Service | Container resources | mitigate | Resource limits (cores, memory, disk) set via Terraform variables |
</threat_model>

<verification>
- `ssh root@test.app1.app.musa.lan hostname` returns hostname
- `ssh root@test.app2.app.musa.lan hostname` returns hostname
- `ssh root@test.bak.backup.musa.lan hostname` returns hostname
- `docker ps` exits 0 on all 3 hosts
- `just test::validate` shows Docker OK + Compose OK on all 3 hosts
- Ansible inventory exists at `ansible/inventory/test.yaml`
</verification>

<success_criteria>
PROV-02 satisfied: 3-node test LXC cluster provisioned with per-environment SSH key auth (no CA).
PROV-03 satisfied: SSH connectivity validated to all 3 test LXCs.
Docker functional inside each LXC (nesting=true verified).
</success_criteria>

<output>
After completion, create `.planning/phases/08-lxc-provisioning-validation/08-01-SUMMARY.md`
</output>
