# Phase 8: LXC Provisioning & Validation - Context

**Gathered:** 2026-05-02
**Status:** Ready for planning
**Mode:** Auto-generated (infrastructure phase — discuss skipped)

<domain>
## Phase Boundary

3 test LXC containers are running on Proxmox and accessible via SSH using per-environment key pairs. Covers PROV-02, PROV-03.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion — pure infrastructure phase. Use ROADMAP phase goal, success criteria, and codebase conventions to guide decisions.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `terraform/envs/test/main.tf` — Full 3-node test configuration (VMIDs 1190-1192, dual NIC)
- `terraform/main.tf` — Musa module with lxc_app + lxc_bak tier split
- `test.just` — Existing recipes: init, plan, apply, deploy, validate
- `lib/infrastructure/terraform/modules/lxc` — Shared LXC module v1.5.0 (CA now optional)

### Established Patterns
- `just test::full` runs init → plan → apply → deploy pipeline
- `just test::validate` checks service health
- Ansible inventory auto-generated by Terraform (custom multi-group per D-09)
- SSH key auth via `ssh_public_key` from base-infra module

### Integration Points
- `tofu apply` creates LXCs → generates Ansible inventory → enables `just test::deploy`
- DNS entries output consumed by hub repository for Pi-hole registration
- SSH connectivity required before Ansible can deploy Docker + services

</code_context>

<specifics>
## Specific Ideas

No specific requirements — infrastructure phase. Key constraint: Phase 7 removed CA dependency, so `tofu apply` should complete without step-ca infrastructure being available.

</specifics>

<deferred>
## Deferred Ideas

None — infrastructure phase.

</deferred>
